<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Text"
        android:id="@+id/showTextButton"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:textColor="#f00"
        android:background="#00f"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ImageText"
        android:id="@+id/showImageButton"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:background="#0e0"/>

    <CalendarView
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:id="@+id/calendarView"
        android:layout_below="@+id/showTextButton"
        android:layout_marginTop="10dp"
        android:shownWeekCount="5"
        android:firstDayOfWeek="2"
        android:selectedWeekBackgroundColor="#f00"
        android:focusedMonthDateColor="#0f0"
        android:weekSeparatorLineColor="#00f"
        android:weekNumberColor="#0ff"
        android:unfocusedMonthDateColor="#0aa"
        />

</RelativeLayout>
